/// Design system spacing constants (2026 refresh).
///
/// Use these instead of raw numbers for consistent layout.
class AppSpacing {
  AppSpacing._();

  /// Page horizontal padding (24px, was 20px)
  static const double pageHorizontal = 24;

  /// Gap between cards in a list (16px, was 12px)
  static const double cardGap = 16;

  /// Gap between content sections (28px, was 20px)
  static const double sectionGap = 28;
}
